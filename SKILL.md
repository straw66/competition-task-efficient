---
name: competition-task-efficient
description: 高质量、低 Token 地完成算法竞赛、企业赛题或复杂工程任务。负责精简规划、局部代码读取、baseline 复现、方案实现、实验管理、故障诊断、提交审查和长会话交接。用户要求完成赛题、实现比赛任务、优化竞赛代码、复现 baseline 或制作提交版本时使用。
---

# Competition Task Efficient

## 核心目标

完整实现当前赛题任务，同时优先保证：

1. 代码可运行；
2. 方案效果；
3. 实验可复现；
4. 提交符合规则；
5. 减少无效 Token 消耗。

减少的是无效扫描、重复读取、重复总结和无依据试错，不得减少必要分析、测试和验证。

## 启动规则

开始前：

1. 自动读取当前作用域内的 `AGENTS.md`；
2. 检查是否已有任务计划、实验记录、交接文件和稳定版本；
3. 已存在的信息不得重复分析；
4. 只有复杂任务才调用 `planning-with-files`；
5. 不得同时无条件加载所有 Skill。

满足以下任一条件时，调用 `planning-with-files`：

- 任务涉及三个以上强相关文件；
- 需要五个以上主要步骤；
- 需要完成完整赛题闭环；
- 涉及多轮实验和版本管理；
- 需要跨模块开发。

调用时先读取其 `SKILL.md`，只创建一次精简计划。

## 首次读取范围

优先读取：

- 赛题说明；
- README；
- baseline；
- 提交样例；
- 评分说明；
- 数据目录结构；
- 环境配置；
- 当前任务直接相关的源码和配置。

禁止默认扫描整个仓库。

读取规则：

- 先用 `find`、`tree`、`rg`、`grep`、`git status`、`git diff` 定位；
- 再读取相关函数、类和配置附近的局部内容；
- 不重复读取没有变化的文件；
- 大文件只读取结构、字段、形状、统计信息和必要样本；
- 日志只读取报错附近及关键指标；
- 不完整加载大型 CSV、JSON、MAT、NPY、日志和模型文件；
- 不读取与当前任务无关的目录。

## 精简任务计划

计划最多包含六个主要阶段：

1. 赛题理解；
2. 环境和 baseline；
3. 数据与指标分析；
4. 核心方案实现；
5. 验证和优化；
6. 提交与文档。

计划只在以下情况更新：

- 一个主要阶段完成；
- 关键路线发生变化；
- 得到重要实验结论；
- 发现阻塞问题；
- 当前最佳版本发生变化。

禁止每次工具调用、文件修改或小测试后更新计划。

## 执行顺序

必须按照以下顺序推进：

1. 理解评分指标、输入输出和限制；
2. 检查运行环境和已有依赖；
3. 跑通最小 baseline 闭环；
4. 保存 baseline 命令、输出和指标；
5. 找出主要性能瓶颈；
6. 实现最小可验证改进；
7. 运行针对性测试；
8. 与上一稳定版本比较；
9. 通过后形成候选提交；
10. 审查候选提交；
11. 保存可复现命令和结果。

不得在 baseline 尚未跑通时进行大范围重构。

## Skill 条件路由

### diagnosing-bugs

仅在以下情况调用：

- 程序报错；
- 结果异常；
- 本地和线上不一致；
- 修改后性能下降；
- 得分突然为零；
- 单测有效但合并失效；
- 输出文件合法但评分异常。

调用要求：

1. 先稳定复现；
2. 收集证据；
3. 缩小问题范围；
4. 给出根因假设；
5. 用实验验证；
6. 做最小修改；
7. 运行回归测试。

不得用盲目调参代替故障定位。

### codebase-design

仅在以下情况调用：

- 新功能跨三个以上模块；
- 当前结构阻碍正确实现；
- 数据、训练、推理和提交逻辑严重耦合；
- 需要建立明确模块接口。

不得为代码美观进行无关重构。

### tdd

只用于可确定验证的逻辑：

- 数据预处理；
- 字段和格式；
- 索引和排序；
- 去重；
- 匹配逻辑；
- 边界条件；
- 提交生成；
- 训练和推理一致性。

模型精度探索、黑盒调参和排行榜实验不强制使用 TDD。

### review

仅在形成候选提交版本时调用。

检查：

- Git 差异；
- 逻辑错误；
- 数据泄漏；
- 答案硬编码；
- 规则违规；
- 训练推理不一致；
- 提交格式；
- 路径和依赖；
- 随机种子；
- 边界条件；
- 潜在回归；
- 无关修改。

只报告有证据的问题，不为凑数量制造建议。

### handoff

仅在以下情况调用：

- 会话明显变长；
- 开始重复读取或重复分析；
- 准备开启新会话；
- 已完成多轮实验；
- 上下文接近难以管理。

交接记录只保留：

- 当前目标；
- 当前最佳版本；
- 已完成工作；
- 修改文件；
- 关键参数；
- 运行命令；
- 本地指标和线上分数；
- 已失败方案及原因；
- 禁止重复的实验；
- 未解决问题；
- 下一步最优行动。

## 实验管理

每次实验只记录：

- 版本号；
- 相比上一版的改动；
- 修改文件；
- 核心参数；
- 本地指标；
- 线上分数；
- 运行时间；
- 结论；
- 是否保留。

实验规则：

- 每次优先只改变一个主要因素；
- 修改前先查历史实验；
- 已确认失败的方案不得原样重复；
- 不以猜测代替结果；
- 没有执行测试时不得声称通过；
- 没有指标时不得声称提升；
- 保存当前最佳版本，不得被未验证版本覆盖。

## Token 控制规则

- 先定位，再读取；
- 先局部，再扩大范围；
- 先快速测试，再完整测试；
- 不重复背景说明；
- 不重复已确认结论；
- 不重复读取未修改文件；
- 不输出未修改的大段代码；
- 不生成无必要文档；
- 不保留冗长思考过程；
- 不同时调用多个无关 Skill；
- 不因为节省 Token 而跳过必要验证。

## 每次回复格式

只汇报以下内容：

### 当前阶段

一句话说明当前进度。

### 本次改动

列出算法变化和代码变化。

### 修改文件

只列实际修改的文件。

### 与上一版本差异

必须说明算法、代码、输出和预期影响的变化。

### 验证结果

报告实际执行的测试、指标和错误；未运行必须明确说明。

### 当前最佳版本

给出版本名、指标和对应文件。

### 下一步

只给出一个最优下一步行动。

避免寒暄、重复背景、泛泛建议和冗长总结。

## 完成标准

只有同时满足以下条件才能宣布任务完成：

- 主流程可以运行；
- 输出格式正确；
- 必要测试通过；
- 结果可复现；
- 提交文件已生成；
- 与评分规则不存在明显冲突；
- 关键命令、参数和结果已经记录。

如果无法全部完成，应交付当前可运行成果，并明确剩余阻塞，不得假装完成。
