# AGENTS.md 示例

## 工作模式

在保证代码质量、可运行性、结果效果和可复现性的前提下，减少无效 Token 消耗。

## 文件读取

- 先使用 `find`、`tree`、`rg`、`grep`、`git diff` 定位相关文件；
- 不默认扫描整个仓库；
- 优先读取相关函数和配置附近的局部代码；
- 不重复读取未修改文件；
- 大型数据文件只读取字段、结构、统计和少量样本；
- 日志只读取报错附近和关键指标。

## 任务规划

- 简单任务直接执行；
- 复杂任务使用 `planning-with-files`；
- 计划最多六个阶段；
- 只在主要阶段完成后更新计划；
- 不在每次工具调用后更新计划。

## Skill 使用

- 报错或结果异常：`diagnosing-bugs`
- 多模块设计：`codebase-design`
- 确定性逻辑验证：`tdd`
- 候选版本审查：`review`
- 长会话交接：`handoff`

不要同时无条件调用所有 Skill。

## 修改与测试

- 优先做最小必要修改；
- 不进行无关重构；
- 先运行快速测试，再运行完整测试；
- 未测试时不得声称通过；
- 未获得指标时不得声称提升；
- 保存当前最佳版本；
- 不覆盖未经验证的稳定结果。

## 实验记录

每次实验记录：

- 版本号
- 算法改动
- 修改文件
- 关键参数
- 本地指标
- 线上分数
- 结论
- 是否保留

不要重复执行已经确认无效的实验。

## 回复格式

每次只汇报：

1. 当前阶段
2. 本次改动
3. 修改文件
4. 与上一版本差异
5. 验证结果
6. 当前最佳版本
7. 下一步

避免重复背景、冗长过程说明和无关建议。
